# Official site (local preview)

Open `index.html` in a browser, or serve this directory with `python -m http.server 8080` and open `http://localhost:8080/`.

This static page reflects the repository's development status as of 2026-09-23. The source link points to the public GitHub repository. The preview download and checksum links target the `v0.1.0-preview` release; verify that both assets are publicly reachable after publication. The page describes a development preview rather than a completed Windows 98 distribution.
